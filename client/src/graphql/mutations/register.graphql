mutation Register(
  $username: String!
  $fullName: String!
  $password: String!
  $email: String!
) {
  register(
    fullName: $fullName
    username: $username
    email: $email
    password: $password
  ) {
    user {
      id
      username
    }
    validationErrors {
      field
      message
    }
  }
}
