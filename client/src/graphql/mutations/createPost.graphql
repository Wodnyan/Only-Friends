mutation CreatePost($title: String!, $description: String!) {
  createPost(article: { title: $title, description: $description }) {
    id
    title
    description
    createdAt
    updatedAt
    user {
      ...authUser
    }
  }
}
